{
  "name": "dependency-detection-action",
  "version": "1.0.0",
  "description": "GitHub Action to detect new dependencies in pull requests",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "js-yaml": "^4.1.0",
    "toml": "^3.0.0",
    "ini": "^5.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "jest": "^29.7.0"
  }
}